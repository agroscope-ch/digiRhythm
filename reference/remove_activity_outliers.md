# Remove outliers from the data

Remove outliers from the data

## Usage

``` r
remove_activity_outliers(df)
```

## Arguments

- df:

  The dataframe containing the activity data

## Value

return a dataframe where columns start the second one have undergone an
outlier removal.
